{
    "plan": [
        {
            "name": "user_service",
            "description": "User service related",
            "options": {
                "deploy": {
                    "type": "remote",
                    "server": "user-service-server",
                    "stages": [
                        "ls -a",
                        "/usr/bin/whoami"
                    ]
                },
                "status": {
                    "type": "remote",
                    "server": "user-service-server",
                    "stages": [
                        "mkdir test",
                        "cd test && echo \"test\" > test"
                    ]
                }
            }
        }
    ]
}